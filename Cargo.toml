[package]
authors = ["Martin Ždila <martin.zdila@freemap.sk>"]
categories = ["compression", "external-ffi-bindings", "science"]
description = "Safe and idiomatic Rust wrapper for Esri's LERC compression library"
documentation = "https://docs.rs/lerc"
edition = "2024"
keywords = ["lerc", "compression", "raster", "geospatial", "ffi"]
license = "Apache-2.0"
name = "lerc"
readme = "README.md"
repository = "https://github.com/FreemapSlovakia/lerc"
rust-version = "1.85.0"
version = "0.2.1"

[dependencies]
lerc-sys = "0.1"
wasm-bindgen = { version = "0.2", optional = true }
js-sys = { version = "0.3", optional = true }

[patch.crates-io]
lerc-sys = { git = "https://github.com/urschrei/lerc-sys", branch = "main" }

[features]
default = ["vendored"]
vendored = []
wasm = ["wasm-bindgen", "js-sys"]

[lib]
crate-type = ["rlib"]

# Uncomment for WASM cdylib output (used by wasm-pack):
# crate-type = ["cdylib", "rlib"]
